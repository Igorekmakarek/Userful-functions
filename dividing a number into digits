//Разделить число на разряды зная количество цифр
//что бы узнать количество цифр в числе не приводя к типу данных string использовать шаблон в этом же репозитории

//в качестве аргументов указатель на чистый массив и количество разрядов в числе
void func (int *p, int k, int N) {
    for (int i = 0; i < k; i++) {
            *(p + i) = N % (int)pow(10, k - i) / pow (10, k - (i + 1));
    }
}

int main () {
  int num[5];
    int *p = num;
    func(p, 5, N);
}
